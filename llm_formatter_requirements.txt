# Note: LLM Formatter Requirements

The LLM Formatter uses Jinja2 for template rendering. 
Add the following to your requirements.txt:

jinja2>=3.1.0
pyyaml>=6.0

These are required for the template-based formatting features.
